site_name: k3cacheable
site_description: Cache data which access frequently
site_url: https://k3cacheable.readthedocs.io
repo_url: https://github.com/pykit3/k3cacheable
repo_name: pykit3/k3cacheable

theme:
  name: material
  palette:
    primary: blue
    accent: blue

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [.]
          options:
            show_source: true
            show_root_heading: true
            heading_level: 2

nav:
  - Home: index.md

markdown_extensions:
  - admonition
  - pymdownx.highlight
  - pymdownx.superfences
